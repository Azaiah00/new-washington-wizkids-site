/**
 * Schema Index
 * This file exports all your content types (schemas)
 * 
 * Think of schemas like "templates" for your content
 * Each schema defines what fields a piece of content has
 */

import blogPost from './blogPost'

// Export all schemas
// When you add more content types (like podcasts, merch, etc.), add them here
export const schemaTypes = [blogPost]


